// Hello World in Javascript 

console.log('Hello World');

// Practicing with substitutions 

const place = 'World';
const greeting = "Hello!";
console.log("%s, %s", greeting, place);

// Practicing with template literals - using same variables as above
// Beteter than using substitutions because it's easier to read as a programmer

console.log(`${greeting}, ${place}`);

